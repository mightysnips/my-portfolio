// DATA FOR PROJECTS
const projectData = {
    vivek: {
        title: "Project VIVEK",
        content: `
            <h3 class="section-title">ABSTRACT</h3>

  <p class="abstract-text">
    <em>
      <strong>Justice delayed</strong> is not merely justice denied;  
      it is <strong>justice eroded</strong>.
    </em>
  </p>

  <p>
    India’s judiciary, burdened by an unprecedented backlog, stands at a moment where
    <strong>reform is no longer optional but inevitable</strong>.
    Artificial Intelligence offers <em>speed, scale, and pattern</em>—but without governance,
    it also carries <strong>opacity and bias</strong>.
  </p>

  <p>
    This work argues that judicial AI must be guided not by efficiency alone,
    but by a balance of
    <em><strong>correctness, fairness, and constitutional restraint</strong></em>.
  </p>

  <hr />

  <h3 class="section-title">INTRODUCTION</h3>

  <p>
    The Indian judiciary was designed to <em>endure</em>, not to hurry—yet delay has become its
    <strong>quiet undoing</strong>.
    With millions of cases awaiting resolution, time itself has turned
    <em>adversarial</em>:
    disadvantaging the poor, unsettling commerce, and thinning public trust.
  </p>

  <p>
    Technology now enters this space not as a <strong>saviour</strong>, but as a
    <em>question</em>—whether the pursuit of speed can coexist with the discipline of justice.
  </p>

  <hr />

  <h3 class="section-title">THE CENTRAL TENSION</h3>

  <p>
    Judicial systems answer to <strong>two masters</strong>.
    One demands <em>correctness</em>, where a single error carries
    <strong>irreversible human cost</strong>.
    The other demands <em>efficiency</em>, where delay weakens governance and deters growth.
  </p>

  <p>
    Artificial Intelligence intensifies this tension.
    Optimised for <strong>speed</strong>, it risks injustice;
    restrained without purpose, it solves nothing.
  </p>

  <p class="highlight-line">
    <em>The challenge is not choice—but reconciliation.</em>
  </p>

  <hr />

  <h3 class="section-title">A GOVERNANCE-ORIENTED VIEW</h3>

  <p>
    AI in adjudication must serve as <strong>counsel</strong>, not command.
    Its reasoning must be <em>visible</em>,
    its influence <em>bounded</em>,
    and its outcomes <strong>accountable</strong>.
  </p>

  <p>
    Human judgement remains the <strong>final authority</strong>,
    while technology assists with
    <em>clarity, consistency, and scale</em>.
  </p>

  <p>
    Only through <strong>structured oversight</strong> and
    <em>holistic evaluation</em>
    can AI strengthen, rather than distort, judicial function.
  </p>

  <hr />

  <h3 class="section-title">CONCLUSION</h3>

  <p class="quote-line">
    <em>
      <strong>The algorithm’s gavel need not be blind—</strong>
      but it must be governed.
    </em>
  </p>

  <p>
    When guided by principle, AI can <strong>shorten delay</strong> without thinning justice,
    and <strong>accelerate process</strong> without eroding trust.
  </p>

  <p>
    The future of judicial reform lies not in <em>automation</em>,
    but in <strong>augmentation</strong>—where law moves faster,
    yet remains anchored to
    <em>fairness, reason, and restraint</em>.
  </p>

</section>
            <div class="quote">"Justice moves slowly not from weakness, but from care." — DS Writers, IFQM 2025</div>
            <a href="https://www.linkedin.com/posts/dhaarmi-sharma_from-a-spark-of-an-idea-to-the-stage-at-activity-7375581143804416000-sxeQ" target="_blank" class="vid-link">Watch Presentation Video 🎥</a>
        `
    },
    vitalink: {
        title: "Vitalink HealthCare",
        content: `<h4>Overview</h4>
        <section class="project-writeup">

  <h2 class="section-title">PROJECT OVERVIEW</h2>

  <p class="abstract-text">
    <em>
      <strong>Healthcare systems do not fail from lack of innovation—</strong>
      they fail from lack of integration.
    </em>
  </p>

  <p>
    <strong>Vitalink</strong> is a cross-platform healthcare application designed to
    bridge patients and doctors through a
    <em>secure, scalable, and responsive digital ecosystem</em>.
    Built using <strong>Flutter</strong> with <strong>Firebase</strong> as its backend,
    the system prioritizes reliability, accessibility, and user-centric design
    across platforms.
  </p>

  <hr />

  <h2 class="section-title">SYSTEM FOUNDATION</h2>

  <p>
    At its core, Vitalink adopts a <strong>full-stack architecture</strong>
    where performance and security are treated as first principles.
    User authentication is implemented using
    <em><strong>Firebase Authentication</strong></em>,
    enabling controlled access through email–password credentials while ensuring
    data integrity and session safety.
  </p>

  <p>
    The application’s data layer is structured using
    <strong>Cloud Firestore</strong>,
    a scalable NoSQL database designed to manage
    <em>distinct patient and doctor identities</em>.
    This separation allows role-specific data modeling,
    ensuring clarity, extensibility, and long-term maintainability.
  </p>

  <hr />

  <h2 class="section-title">ROLE-BASED INTERACTION</h2>

  <p>
    Vitalink is not a single interface—it is a
    <strong>context-aware system</strong>.
    Patients and doctors experience the platform through
    <em>distinct dashboards</em>,
    each tailored to their responsibilities, workflows, and expectations.
  </p>

  <p>
    This role-based design ensures that information is
    <strong>presented with intent</strong>,
    reducing cognitive load while improving usability and trust.
    The system adapts not only to the user, but to the
    <em>purpose of their interaction</em>.
  </p>

  <hr />

  <h2 class="section-title">INTERFACE & EXPERIENCE</h2>

  <p>
    The user interface is designed to be
    <strong>dynamic, responsive, and expressive</strong>.
    Vitalink supports multiple visual themes,
    including <em>light and dark modes</em>,
    allowing the experience to adapt to user preference and environment.
  </p>

  <p>
    Custom widgets are developed entirely from the ground up,
    emphasizing modularity and visual coherence.
    A standout element is a
    <em><strong>3D rotating logo</strong></em>,
    implemented using <strong>AnimationController</strong> and
    <strong>CustomPainter</strong>,
    serving as both a visual signature and an interactive engagement anchor.
  </p>

  <hr />

  <h2 class="section-title">DESIGN PHILOSOPHY</h2>

  <p class="highlight-line">
    <em>
      Technology in healthcare must feel dependable before it feels impressive.
    </em>
  </p>

  <p>
    Vitalink reflects a philosophy where
    <strong>functionality precedes flair</strong>,
    yet design remains intentional and human-centered.
    Every animation, transition, and interaction is engineered
    not for novelty, but for <em>confidence and clarity</em>.
  </p>

  <hr />

  <h2 class="section-title">CONCLUSION</h2>

  <p class="quote-line">
    <em>
      <strong>Vitalink is not merely an app—</strong>
      it is an interface of trust.
    </em>
  </p>

  <p>
    By combining secure backend architecture,
    role-aware interaction design,
    and thoughtfully crafted user experiences,
    Vitalink demonstrates how modern mobile technologies can
    <strong>support healthcare without overwhelming it</strong>.
  </p>

  <p>
    The project stands as an exploration of how
    <em>engineering discipline, design restraint, and user empathy</em>
    can converge to build systems that are
    reliable, scalable, and meaningfully human.
  </p>

</section>`
            
    },
    path: {
        title: "Shortest Path Navigator",
        content: `<h4>Overview</h4>
        <section class="project-writeup">

  <h2 class="section-title">PROJECT OVERVIEW</h2>

  <p class="abstract-text">
    <em>
      <strong>Navigation is not about movement—</strong>
      it is about decision.
    </em>
  </p>

  <p>
    <strong>Shortest Path Navigator</strong> is a system designed to model real-world
    road networks as structured data and transform them into
    <em>optimal, actionable routes</em>.
    Built in <strong>C</strong> and grounded in
    <strong>Dijkstra’s Algorithm</strong>,
    the project focuses on correctness, efficiency, and interpretability
    in path-finding systems.
  </p>

  <hr />

  <h2 class="section-title">GRAPH REPRESENTATION</h2>

  <p>
    The navigation space is formalized as a
    <strong>weighted graph</strong>,
    where nodes represent
    <em>locations such as cities or intersections</em>,
    and edges represent
    <strong>roads with associated cost</strong>
    in terms of distance or time.
  </p>

  <p>
    This graph is implemented using
    <em>adjacency lists or adjacency matrices</em>,
    enabling efficient storage and traversal
    while preserving structural clarity.
    The abstraction allows real-world geography
    to be reasoned about algorithmically.
  </p>

  <hr />

  <h2 class="section-title">ALGORITHMIC CORE</h2>

  <p>
    At the heart of the system lies
    <strong>Dijkstra’s Algorithm</strong>,
    implemented with a
    <em>priority queue</em>
    to ensure optimal time complexity.
    From a given source node,
    the algorithm computes the shortest distance
    to all reachable nodes with
    <strong>deterministic correctness</strong>.
  </p>

  <p>
    Each relaxation step refines the system’s understanding
    of the network,
    ensuring that no shorter path remains undiscovered.
    The result is not just a route,
    but a proof of optimality.
  </p>

  <hr />

  <h2 class="section-title">ROUTE VISUALIZATION</h2>

  <p>
    Computation alone is insufficient without interpretation.
    Once the shortest path is determined,
    the resulting route is translated into a
    <strong>structured URL</strong>
    that interfaces with
    <em>Google Maps</em>.
  </p>

  <p>
    This integration bridges algorithmic output
    with real-world navigation,
    transforming abstract node sequences
    into a
    <strong>visual, human-readable route</strong>.
    The system thus connects low-level computation
    with high-level usability.
  </p>

  <hr />

  <h2 class="section-title">DESIGN PHILOSOPHY</h2>

  <p class="highlight-line">
    <em>
      An algorithm is only complete when its result can be trusted—and understood.
    </em>
  </p>

  <p>
    Shortest Path Navigator emphasizes
    <strong>algorithmic rigor over approximation</strong>,
    while ensuring that outcomes remain accessible
    beyond the code itself.
    The project demonstrates how classical algorithms,
    when carefully implemented,
    remain deeply relevant to modern navigation systems.
  </p>

  <hr />

  <h2 class="section-title">CONCLUSION</h2>

  <p class="quote-line">
    <em>
      <strong>The shortest path is not guessed—</strong>
      it is proven.
    </em>
  </p>

  <p>
    By combining precise graph modeling,
    a priority-queue-optimized implementation
    of Dijkstra’s Algorithm,
    and real-world visualization through mapping APIs,
    the project illustrates how
    <strong>theory, systems, and usability</strong>
    can converge into a cohesive solution.
  </p>

  <p>
    Shortest Path Navigator stands as a study in
    <em>how foundational algorithms scale
    from textbooks to terrain</em>.
  </p>

</section>
     <a href="https://www.linkedin.com/in/dhaarmi-sharma/overlay/Project/798012836/treasury/?profileId=ACoAAE7sg5MBxMQT8AL8-1jXJZNQj6fq_lN2bf4" target="_blank" class="vid-link">View Post regarding SPN </a>`
    }
};

window.addEventListener('load', () => {
    const loader = document.getElementById('loader');
    const avatar = document.getElementById('avatarWrapper');
    
    setTimeout(() => {
        loader.style.opacity = '0';
        setTimeout(() => {
            loader.style.display = 'none';
            avatar.style.opacity = '1';
            setTimeout(() => {
                avatar.classList.add('shrink');
                startDenseSparks();
            }, 2000);
        }, 1000);
    }, 1500);

    // Avatar Click
    document.getElementById('avatarTrigger').addEventListener('click', (e) => {
        e.stopPropagation();
        document.getElementById('linksCard').classList.toggle('show');
    });
    document.addEventListener('click', () => {
        document.getElementById('linksCard').classList.remove('show');
    });

    // Mode Toggle
    document.getElementById('modeToggle').addEventListener('click', function() {
        document.body.classList.toggle('matrix-mode');
        this.innerHTML = document.body.classList.contains('matrix-mode') ? "Go Light ☀️" : "Go Dark 👾";
        if(document.body.classList.contains('matrix-mode')) initMatrix();
    });
});

// PANEL LOGIC
function openProject(key) {
    const data = projectData[key];
    document.getElementById('panelContent').innerHTML = `<h2>${data.title}</h2>${data.content}`;
    document.getElementById('projectPanel').classList.add('open');
}

function closeProject() {
    document.getElementById('projectPanel').classList.remove('open');
}

// SPARKS
function startDenseSparks() {
    const end = Date.now() + 1500;
    const interval = setInterval(() => {
        if (Date.now() > end) { clearInterval(interval); return; }
        const avatar = document.getElementById('avatarWrapper');
        const rect = avatar.getBoundingClientRect();
        for(let i=0; i<4; i++) createSpark(rect.left + rect.width/2, rect.top + rect.height/2);
    }, 40);
}

function createSpark(x, y) {
    const spark = document.createElement('div');
    spark.className = 'spark';
    document.body.appendChild(spark);
    const offX = (Math.random() - 0.5) * 60;
    const offY = (Math.random() - 0.5) * 60;
    spark.style.left = (x + offX) + 'px'; spark.style.top = (y + offY) + 'px';
    spark.style.background = ['#FFD700', '#FFFFFF', '#00ff41'][Math.floor(Math.random() * 3)];
    setTimeout(() => spark.remove(), 1000);
}

// MATRIX & THEME
function updateTheme() {
    const hour = new Date().getHours();
    const celestial = document.getElementById('celestialBody');
    if (!celestial) return;
    document.body.classList.remove('morning', 'afternoon', 'evening', 'night');
    celestial.classList.remove('sun', 'moon');

    if (hour >= 5 && hour < 12) { document.body.classList.add('morning'); celestial.classList.add('sun'); celestial.style.top="40%"; }
    else if (hour >= 12 && hour < 17) { document.body.classList.add('afternoon'); celestial.classList.add('sun'); celestial.style.top="10%"; }
    else if (hour >= 17 && hour < 20) { document.body.classList.add('evening'); celestial.classList.add('sun'); celestial.style.top="60%"; }
    else { document.body.classList.add('night'); celestial.classList.add('moon'); celestial.style.top="20%"; }
}
updateTheme();
setInterval(updateTheme, 60000);

let matrixInterval;
function initMatrix() {
    if(matrixInterval) clearInterval(matrixInterval);
    const canvas = document.getElementById('matrixCanvas');
    if(!canvas) return;
    const ctx = canvas.getContext('2d');
    canvas.width = window.innerWidth; canvas.height = window.innerHeight;
    const letters = "010101010111001";
    const fontSize = 16;
    const columns = canvas.width / fontSize;
    const drops = [];
    for (let i = 0; i < columns; i++) drops[i] = 1;
    function draw() {
        ctx.fillStyle = "rgba(0, 0, 0, 0.05)";
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        ctx.fillStyle = "#00ff41";
        ctx.font = fontSize + "px monospace";
        for (let i = 0; i < drops.length; i++) {
            const text = letters.charAt(Math.floor(Math.random() * letters.length));
            ctx.fillText(text, i * fontSize, drops[i] * fontSize);
            if (drops[i] * fontSize > canvas.height && Math.random() > 0.975) drops[i] = 0;
            drops[i]++;
        }
    }
    matrixInterval = setInterval(draw, 33);
}

// FIXED SKILL ANIMATION
// REPLACE your existing skillObserver block at the end of script.js with this:
const skillObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            const rows = entry.target.querySelectorAll('.skill-row');
            rows.forEach(row => {
                const fill = row.querySelector('.fill');
                const level = row.getAttribute('data-level');
                if (fill && level) {
                    fill.style.width = level;
                }
            });
        }
    });
}, { threshold: 0.2 });

document.querySelectorAll('.skills-category').forEach(cat => {
    skillObserver.observe(cat);
});

// Add this to your script.js
const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            const fills = entry.target.querySelectorAll('.progress-fill');
            fills.forEach(fill => {
                const parent = fill.closest('.skill-row');
                fill.style.width = parent.getAttribute('data-width');
            });
        }
    });
}, { threshold: 0.5 });

document.querySelectorAll('.skills-category').forEach(cat => {
    observer.observe(cat);
});